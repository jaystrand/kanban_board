{
  "name": "main",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "client:build": "cd client && npm ci && npm run build",
    "server:build": "cd server && npm run build",
    "start": "npm run client:build && npm run server:build && npm run server:start",
    "server:start": "cd server && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.9.1",
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.7",
    "wait-on": "^7.2.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.13.1"
  }
}
